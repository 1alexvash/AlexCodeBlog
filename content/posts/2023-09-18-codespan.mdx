---
title: Enhance Code Sharing with CodeSnap VSCode Extension
date: 2023-09-21T00:00:00.000Z
draft: true
tags:
  - VSCode
  - Productivity
---

When the time comes to share your code with someone else, taking a screenshot isn't always the best idea.

```javascript
function toHumanReadableDate(ISODate: Date | string) {
  const date = new Date(ISODate);

  const dateTimeFormat = new Intl.DateTimeFormat("en-US", {
    timeZone: "UTC",
    year: "numeric",
    month: "long",
    day: "numeric",
  });

  return dateTimeFormat.format(date);
}

export default toHumanReadableDate;
```

It doesn't look great, right? I think, the better idea will be using [CodeSnap](https://marketplace.visualstudio.com/items?itemName=adpyke.codesnap) VSCode extension.

![](/post-images/condesnap-example.jpg)

That's much better 🤩 It offers a much more polished appearance with smooth corners and a clean, simple background.

I hope you find this extension as useful as I do!
